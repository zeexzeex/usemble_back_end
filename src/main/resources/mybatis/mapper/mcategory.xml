<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mycompany.webapp.dao.McategoryDao">
	<select id="getMcategory" parameterType="String" resultType="Category">
		select mid, ctname, ctno
		from mcategory mc, category c
		where mid= #{mid} and mc.ctno = c.ctno 
	</select>
	
	<insert id="putCategory" parameterType="String">
		insert into mcategory(mid, ctno)
		values (#{mid}, #{ctno})
	</insert>
</mapper>
